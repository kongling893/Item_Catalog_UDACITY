{% extends "index.html" %}
{% block content %} 
	<div class="row divider blue">
		<div class="col-md-12"></div>
	</div>
	<div class="row banner menu">
		<div class="col-md-11 col-md-offset-1 padding-none">
			<h1>{{ toyshop.name }}
			<figure class="creator">
				<img src= ' {{login_session['picture']}} ' style = "width: 30px; height: 30px;border-radius: 5px;-webkit-border-radius: 15px;-moz-border-radius: 5px;">
				<figcaption>Menu creator's name goes here</figcaption>

			</figure>
			</h1>

		</div>
	</div>

<div class = 'flash'>
		      {% with messages = get_flashed_messages() %}
      {% if messages %}

        <ul>
        {% for message in messages %}
            <li> <strong> {{ message }} </strong> </li>
        {% endfor %}
        </ul>
        {% endif %}
    {% endwith %}


	{% if toys !=[] %}
		<div class="row">
			<div class="col-md-1"></div>
			<div class="col-md-3">
				{% for toy in toys %}
						<div class="menu-item">
							<h3>{{toy.name}}</h3>
							<p>{{toy.description}}</p>
							<p class="menu-price">{{i.price}}</p>
							<a href='{{url_for('editMenuItem', food_truck_id = food_truck.id, menu_id=i.id ) }}'>Edit</a> |
							<a href='{{url_for('deleteMenuItem', food_truck_id = food_truck.id, menu_id=i.id ) }}'>Delete</a>
						</div>
				{% endfor %}
			</div>
			<div class="col-md-1"></div>
		</div>
	{% endif %}
{% endblock %}
